<%= form_with(model: adquisicione, local: true) do |form| %>
  <% if adquisicione.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(adquisicione.errors.count, "error") %> prohibited this adquisicione from being saved:</h2>

      <ul>
      <% adquisicione.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :fecha_compra %>
    <%= form.date_select :fecha_compra, id: :adquisicione_fecha_compra %>
  </div>

  <div class="field">
    <%= form.label :monto %>
    <%= form.text_field :monto, id: :adquisicione_monto %>
  </div>

  <div class="field">
    <%= form.label :cliente %>
    <%= form.text_area :cliente, id: :adquisicione_cliente %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
